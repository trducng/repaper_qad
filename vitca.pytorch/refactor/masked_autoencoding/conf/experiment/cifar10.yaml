# @package _global_
defaults:
  - override /dataset: cifar10
  - override /model: vitca

experiment:
  name: cifar10

  batch_size:
    train: 32
    val: 32

  input_size:
    train: [32, 32]
    val: [32, 32]

  val_frequency: 1000

  iter:
    train:
      ca:
        min: 8
        max: 32

  masking:
    train:
      type: dropout
      max_prob: 0.75
      max_patch_shape: [4, 4]
      prob_stages: 3
      patch_shape_stages: 3
    val:
      type: dropout
      max_prob: 0.75
      max_patch_shape: [4, 4]
      prob_stages: 3
      patch_shape_stages: 3